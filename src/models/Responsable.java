package models;

public class Responsable extends User{
    public Responsable(long id, String nom, String prenom, String login, String role, String password) {
        super(id, nom, prenom, login, role, password);
    }
}
